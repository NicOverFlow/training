package org.example.utils;

public class Node {
    int data;      // Valeur du nœud
    Node next;     // Référence vers le nœud suivant

    public Node(int data) {
        this.data = data;
        this.next = null;
    }
}
